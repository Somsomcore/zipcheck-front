<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_zipcheck"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_zipcheck_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Zipcheckfront"
        android:name=".ui.ZipCheckApplication"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true">
        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="@string/NATIVE_APP_KEY" />
        <meta-data
            android:name="com.naver.sdk.clientId"
            android:value="@string/Naver_Client_ID" />

        <meta-data
            android:name="com.naver.sdk.clientSecret"
            android:value="@string/Naver_Client_SECRET" />

        <meta-data
            android:name="com.naver.sdk.clientName"
            android:value="@string/Naver_App_Name" />
        <activity
            android:name=".ui.screen.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Zipcheckfront">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>